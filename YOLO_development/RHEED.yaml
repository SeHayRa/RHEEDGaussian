# data.yaml

path: ../datasets/RHEED/  # Path to the dataset directory
train: images/train  # Relative path to the train image directory
val: images/val  # Relative path to the validation image directory (optional)


nc: 1  # Number of classes (change as per your dataset)
names: ['Gaussian']  # Class names
